# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh
<!-- remove the add button from both order component, product component 
and also add courier. without the add button the other screens attached to the add button wont display ,i dont know the component or files unless i see and test them so just remove the buttons later i could remove the redundant files.
when you hover on the card on order component that 3dotted icon visible at the right end i dont think we need that too.you can check and see it... i feel if we cant mutate and order we dont need that .... 
The call and messaging under the courier card you could remove it or disable the call icon and messaging ,

-->
